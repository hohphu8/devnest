# DevNest 0.2.19

## Fixed

- Release notes in Settings now render Markdown headings, lists, links, quotes, and code with native DevNest styling.
- Remote HTML stays escaped instead of being executed inside the app.

## Improved

- Markdown loads with the lazy Settings route, preserving fast app startup.
